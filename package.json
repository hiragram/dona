{
  "name": "dona",
  "version": "0.1.0",
  "private": true,
  "description": "Local development launcher for Dona",
  "type": "module",
  "engines": {
    "node": ">=20.18.1"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test": "npm --prefix dispatcher test && npm --prefix sources/slack test && npm --prefix updater test",
    "typecheck": "npm --prefix dispatcher run typecheck && npm --prefix sources/slack run typecheck && npm --prefix updater run typecheck",
    "build": "npm --prefix dispatcher run build && npm --prefix sources/slack run build && npm --prefix updater run build",
    "verify": "npm test && npm run typecheck && npm run build"
  }
}
